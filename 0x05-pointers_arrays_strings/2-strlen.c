#include "main.h"
#include <string.h>

/**
  * _strlen - a function that returns the length of a string.
  * @s: input string
  *
  * Return: void.
  */
int _strlen(char *s)
{
	int l;

	l = strlen(s);
	return (l);
}
